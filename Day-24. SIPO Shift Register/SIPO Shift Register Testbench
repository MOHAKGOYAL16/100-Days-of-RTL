module SIPO_tb();
reg clk, shift_in;
wire [3:0]q;

SIPO dut(.clk(clk), .shift_in(shift_in), .q(q));

initial begin
clk = 0;
shift_in = 1;#10;
shift_in = 0;#10;
shift_in = 1;#10;
shift_in = 0;#10;
#50;
$finish;
end
always #5 clk = ~clk;
endmodule
