module SISO_tb();
reg shift_in, clk;
wire shift_out;

SISO dut(.shift_in(shift_in), .clk(clk), .shift_out(shift_out));

initial begin
clk=0;
shift_in = 1 ;#10;
shift_in = 0 ;#10;
shift_in = 1 ;#10;
shift_in = 0 ;#10;
#50;
$finish;
end
always #5 clk = ~clk;
endmodule
