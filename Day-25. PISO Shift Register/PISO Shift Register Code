module PISO(clk,mode,si,so);
input clk, mode;
input [3:0]si;
output so;
assign so = q[0];
reg [3:0]q = 0;
always @(posedge clk)
begin
    if(mode)
        q<=si;
    else
    begin
    q[3] <= 1'b0;
    q[2] <= q[3];
    q[1] <= q[2];
    q[0] <= q[1];
    end
end
endmodule
