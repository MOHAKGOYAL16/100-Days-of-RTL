module D_FF_tb();
reg D, clk, rst;
wire q, q_bar;

D_FF dut(.D(D), .clk(clk), .rst(rst), .q(q), .q_bar(q_bar));

initial begin
clk = 0;
forever #10 clk = ~clk;
end

initial begin
rst = 1; D = 0;#20;
rst = 0; D = 0;#20;
D=1;#20;
D=0;#20;
$finish;
end

endmodule
